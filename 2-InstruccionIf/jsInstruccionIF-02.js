// Oliverio Santiago, Div H
function mostrar()
{
	let edad;
	edad = txtIdEdad.value;
	edad = parseInt(edad);

	if (edad >= 18) 
	{
		alert("Eres mayor de edad.");
	}

}//FIN DE LA FUNCIÓN